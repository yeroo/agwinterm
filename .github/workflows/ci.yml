name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-test:
    name: build + test (windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore Agwinterm.slnx

      - name: Build
        run: dotnet build Agwinterm.slnx -c Release --no-restore

      - name: Check agwinterm-core ABI agreement
        # The C ABI is declared three times (Rust const, C# RequiredAbi, lite kRequiredAbi) and a
        # drift is otherwise only caught at load, by whoever runs the mismatched pair. Cheap to
        # check, and it becomes the cross-repo gate once agliteterm ships from its own repository.
        shell: pwsh
        run: ./tools/check-abi.ps1

      - name: Build Rust core (arms the parity oracle)
        # RustParityTests skip silently when the crate dll is absent; building it here makes
        # the C#↔Rust differential oracles (wcwidth, screen, parser, emulator, images) a
        # hard merge gate.
        run: cargo build --release --manifest-path native/Cargo.toml

      - name: Build agwinterm-lite (the handover build)
        # Compiles the lite client against the Rust core dll + committed nanopb sources, so a
        # protocol/schema drift or C++ break fails the merge. cl.exe is on windows-latest.
        shell: pwsh
        run: ./lite/build.ps1

      # The control API is a cross-product contract now: agliteterm ships from its own repository
      # promising to speak this dialect, and its CI runs THIS spec against its own client. Running
      # it here too is what makes "same control API" a gate rather than a claim — a verb reshaped
      # in the full app fails the other product's build, and vice versa.
      - name: Control-API conformance
        shell: pwsh
        timeout-minutes: 15
        run: ./tests/conformance/conformance.ps1 -Strict

      - name: Test (Core + Pty)
        # One retry for the known .NET 10 test-host #118 pathology under named-pipe churn, which
        # manifests two ways: a native CRASH ("Test Run Aborted") OR a DEADLOCK that hangs the run
        # forever (a pipe teardown that never completes — no crash, no output). The crash-only retry
        # missed the hang and stalled the job for hours, so this runs the suite under a hard timeout
        # and retries once on EITHER symptom. Genuine test failures fail immediately (no retry).
        shell: pwsh
        timeout-minutes: 25
        run: |
          function Invoke-Tests {
            if (Test-Path out.txt) { Remove-Item out.txt, err.txt -ErrorAction SilentlyContinue }
            $p = Start-Process dotnet -ArgumentList 'test','Agwinterm.slnx','-c','Release','--no-build' `
                 -NoNewWindow -PassThru -RedirectStandardOutput out.txt -RedirectStandardError err.txt
            if (-not $p.WaitForExit(360000)) {   # 6 min: the suite is ~30s, so this only trips on a deadlock
              try { $p.Kill($true) } catch {}
              Write-Host '::warning::test run HUNG (issue #118 deadlock variant) - killed'
              return 'retry'
            }
            $out = (Get-Content out.txt, err.txt -Raw -ErrorAction SilentlyContinue) -join "`n"
            if ($p.ExitCode -eq 0) { Write-Host $out; return 'ok' }
            if ($out -match 'Test Run Aborted|host process crashed') {
              Write-Host '::warning::test host crashed natively (issue #118)'; return 'retry'
            }
            Write-Host $out; return 'fail'   # a genuine failure
          }
          $r = Invoke-Tests
          if ($r -eq 'ok') { exit 0 }
          if ($r -eq 'fail') { exit 1 }
          Write-Host '::warning::retrying the test suite once (issue #118)'
          if ((Invoke-Tests) -eq 'ok') { exit 0 } else { exit 1 }
