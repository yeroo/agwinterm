# P4 — splits get their full shape

Batch **P4** of the parity programme in
[2026-09-03-parity-batches.md](2026-09-03-parity-batches.md). Closes items 2 (`session.swap`) and
3 (the three split gaps) of [agterm-parity.md](../agterm-parity.md), as numbered today. The second
restore-format change of the backlog; it inherits P3's rule and P3's test
(`src/Agwinterm.Pty/RestoreState.cs:6-30`, `tests/Agwinterm.Pty.Tests/RestoreStateTests.cs`).

## The vocabulary, fixed before anything is written

agterm's command reference (agterm.com/commands, read 2026-09-06): `session split [on|off|toggle]
[--axis vertical|horizontal]`. **`vertical` produces left/right panes — today's only layout;
`horizontal` produces top/bottom panes.** Omitting `--axis` keeps the current orientation, and a
session that has never been split defaults to `vertical`. `session focus` takes
`primary|split|left|right|top|bottom|other`; `session resize` gains `--grow-top` / `--grow-bottom`
beside `--grow-left` / `--grow-right`. This repo contradicts itself today (`agliteterm/src/main.cpp:6`
calls the side-by-side split "vertical" — agterm's word; `docs/agterm-gap-analysis.md:47` calls it
"horizontal"), so the word is stated **once** in `ISessionHost.Split`'s comment, once in
`AgentSkill.cs`, once in the CLI usage header, and the gap-analysis line is corrected. The axis
names the **arrangement**, never the divider.

Two divergences from agterm, recorded here and in `docs/agterm-parity.md`, not silently:

- **`off` destroys the split shell** (agterm hides it and keeps the process). Already recorded in
  the tracker; unchanged by this batch. `session split close` is therefore the same destruction
  with a pane target — its new capability is closing **either** side (see Overview).
- **A swap moves panes, never ids.** agterm's swap moves the session's public identity to the other
  shell (its panes are addressed by role, `primary|split`). Ours are addressed by **id**, and an
  agent holding a pane id must keep reaching the same shell after a swap. So a swap reverses the
  pane order, keeps the geometry (axis and ratio sequence), moves focus with the pane, and leaves
  every id where it was — the session id keeps naming the pane it always named, which now sits on
  the other side. That relaxes one invariant: "the first pane shares the session id" becomes
  "exactly one pane carries the session id", and `CreateSession` / restore must accept a pane-0 id
  that differs from the session id (Task 5).

## Overview

Four items on one subsystem — a session's two panes — plus the reply that makes them scriptable.

- **`session split` returns the pane id.** Today it answers the constant `"split"` from a
  `Post(...); return true;` (`Program.ControlHost.cs:491-501`), so the caller cannot address the
  shell it just created (lite returns the id, `agliteterm/src/main.cpp:7911-7947`, and the parity
  tracker calls this a gap inside our own family). The host moves to `InvokeOnUiQueued` so the id
  names a pane that exists; **`on` when already split returns the existing split pane's id**
  (lite's rule: a caller that does not know whether it split gets something addressable either
  way); `off` returns the surviving pane's id; `toggle` returns whichever it produced. A bare
  string, so the conformance step (`control-api.json:284-292`, `"result":"string"` on `split off`)
  keeps passing on the type check it makes.
- **An axis.** `session split on --axis vertical|horizontal` (and `toggle`); a session's axis is
  per session (`Ses.Axis`, default vertical), survives restore as an additive `SessionState.Axis`
  key validated on load, and is reported by `tree --json` as `"axis"` inside the `paneCount > 1`
  block — always, like `focusedPane`, because absence would mean "this build has no axis", which
  tells a caller nothing. `--axis` on an already-split session **re-orients it live** (agterm:
  omitting the flag preserves the orientation, so passing it sets it). The layout has one source of
  truth (`PaneLayout`, `Program.Sessions.cs:453-469`) and ~20 sites that assume columns — the list
  in Context is the task's checklist.
- **`session split close [--target <pane id>]`** — closes the targeted pane, **either side**; the
  survivor becomes pane 0 with the full width/height and focus. No target, or a session id → the
  session's focused pane (what `Ctrl+Shift+W` does). A single-pane session is **refused**
  (`session close` is the verb for that; a `split close` that closes a session is the silent-success
  class). Reply: the survivor's id. The tracker says this item is "naming, not behaviour" — it is
  not: no control verb can close pane 0 of two today (`off` hard-codes `Panes[0]` as the survivor,
  `Program.Sessions.cs:1552`).
- **`session swap [--target <session or pane>]`** — exchanges the two panes: order reversed, axis
  kept, the ratio SEQUENCE kept (the left/top box stays the size it was; the contents change
  places), focus follows the pane, overlays/scratch/quick untouched (they are session-wide until P5),
  status untouched (aggregated order-independently — `StatusAggregate.cs:24-55`), every id
  untouched. Reply `{"session","paneIds":[…],"focusedPane","axis"}` (`OkRaw`). A single-pane session
  is refused.

## Context (from discovery)

The model:

- `Pane` `src/Agwinterm.Win32/Program.cs:292-330` — `Id`, `AgentResume`, `RestoreCommand`,
  `CapturedCommand`, `FontSize`, **`Ratio` (:307, "fraction of the session's content width" — each
  pane's OWN share; `PaneLayout` normalises by the sum)**, `ScrollOffset`, `Unread`, `ReadOnly`, the
  selection block — all per pane, so they travel with the pane under a swap. `Ses` `:332-360` —
  `Panes`, `Active` (focused index), `ActivePane`, the `S`/`FontSize`/`StartCwd` shims. `SplitPane`
  caps at two (`Program.Sessions.cs:1199`) but `AppendPane` (`:434-439`) and the restore loop are
  written for N — a hand-edited file yields three panes today.
- First pane shares the session id: `Program.Sessions.cs:421`; fake `FakeSessionHost.cs:30-34`.
- **Positional state is exactly three things**: `Ses.Panes` order, `Ses.Active`, and the ratio
  sequence. No cached rects, no per-side arrays, no per-pane UIA node (`Program.cs:896-904`), no
  per-pane sidebar indicator; MRU and broadcast are session-keyed; events carry pane ids.

Every site that assumes the split is side by side (the axis checklist):

- **Non-mechanical (2)**: `PaneLayout` (`Program.Sessions.cs:453-469`: `avail = totalW -
  (n-1)*DividerW`, `x += w + DividerW`) and `ResizeActiveSplitInternal` (`:1562-1583`: converts
  columns to a ratio shift with cell WIDTH and `totalW` — on `horizontal` it is rows, cell HEIGHT,
  `totalH`).
- Hit-tests, x-only (5): `FocusPaneAtX` (`Program.Input.cs:417-422`), `DividerAtX` (`:424-436`),
  `DragDivider(int px)` (`:438-455`), `PaneAt` (`:546-558`), the wheel's inline test
  (`Program.WndProc.cs:555-568`). `Program.WndProc.cs:490` passes only `DipX` to `DragDivider`.
  `PaneBox`, `ActivePaneView`, `CellAtPx` are already generic.
- Renderer (2): the divider hairline `Program.Render.cs:753-756` and the focused-pane accent bar
  `:759` (a bar along the pane's x span pinned to the title bar — on `horizontal` it must sit on
  the pane's own top edge, or be a left-edge bar; decide and say why).
- Prose/naming: `DividerW` (`Program.cs:127`), `_divLeft` (`:129`), the `Ratio` comment (`:307`),
  `CloseActivePane` `:1243`, `OnPaneProcessExited` `:1263`, `CollapseToSinglePane` `:1552`;
  `DrawSplitGlyph` (`Program.Services.cs:480`, "two side-by-side panes" — the toolbar icon should
  show the session's axis when split); `FocusPane(int dir)` (`:1217`), `focus_left_pane` /
  `focus_right_pane` (`Keymap.cs:31-32,57` — a public config surface, **do not rename; alias**),
  `FocusPaneDir("left"|"right"|"other")` (`Program.ControlHost.cs:502-511`).
- **No cursor shape over the divider exists** (`WM_SETCURSOR`, `Program.WndProc.cs:287-289`, hand
  over links only). `IDC_SIZEWE` for vertical, `IDC_SIZENS` for horizontal is a small addition.
- DPI: nothing — `DipX`/`DipY` convert both axes on the way in.
- Covers are not a site: `CoverRect()` (`Program.Sessions.cs:665-679`) spans the content region.

The verb today:

- CLI `src/Agwinterm.Ctl/Program.cs:276` (`op` = first positional or `toggle`); server
  `ControlServer.cs:254` (session-resolved block, `host.Split(target, op) ? Ok("split") : Err(…)`);
  `ISessionHost.Split` `:211-214` + the #230 comment; host `Program.ControlHost.cs:491-501`
  (resolve synchronously via `FindSesForTarget`, then `Post`); app `SplitOp` `Program.Sessions.cs:1530-1547`,
  `SplitPane` `:1197-1215` (new id = `Guid.NewGuid()`, cwd/font from the current pane, ratios
  halved, `ses.Active = idx+1`, `_session` repointed only if `ses` is `_active`), `CloseActivePane`
  `:1234-1250` (closes the FOCUSED pane either side, redistributes the ratio, clamps `Active`;
  bound to `close_pane` / Ctrl+Shift+W), `CollapseToSinglePane` `:1549-1559` (keeps `Panes[0]`),
  `OnPaneProcessExited` `:1253-1271`. Fake `FakeSessionHost.cs:376-386` (bumps `PaneCount`, never
  mints an id — `AddPane()` `:74-80` does). Tests `ControlApiTests.cs:58-78`, `:313-353`.
- **Degenerate ops today**: `on` when on and `off` when off return `ok:true "split"` having done
  nothing (`SplitOp`'s guards).
- **#230 (`745b11f`) invariants**: resolve on the caller's thread (bad target → `ok:false`, nothing
  posted); the split/collapse/close/swap functions take the session they act on and never read
  `_active`; `_session` is repointed only when the acted-on session IS the active one. A queued hop
  re-resolves inside the hop (`SessionContext`, `ISessionHost.cs:168-172`).
- Resolver: `FindControlPane` `Program.Sessions.cs:1141-1151` — exact pane → exact session → pane
  prefix → session prefix/name; a pane-1 id hits the first arm (pinned by
  `tests/integration/win32-control.ps1:224-234`).

Tree, restore, tests:

- `SessionSnapshot` `ISessionHost.cs:14-19` ("new optional fields go at the END"); `HandleTree`
  `ControlServer.cs:397-459` — the `paneCount > 1` block at `:436-452` emits `paneCount`,
  `focusedPane`, `splitRatios`, `paneIds`; `Tree_OmitsSplitFields_ForSinglePane`
  (`ControlApiTests.cs:72-78`) pins that a single pane stays lean.
- `RestoreState.cs:35` `PaneState` (`Ratio` per pane), `:38-57` `SessionState`; save
  `Program.Services.cs:1400-1447`; restore `:1530-1620` (pane 0 via `CreateSession` with the
  SESSION id, panes 1..n via `AppendPane(pl[i].Id, …)`, then ratios/resume/pin/capture and
  `Active` clamped). **`RestoreStateTests.cs:194-209` uses `"SplitAxis"` and `"Axis"` as its
  unknown-key fixture** — one becomes real in this batch.
- `tests/integration/restore-roundtrip.ps1` (cells `graceful|killed|refusal|replay`, `-Only`),
  `tests/ui/lib.ps1` `Restart-Sandbox [-Kill]` (`:221-248`), `Save-SandboxCapture` / `Compare-Capture`
  (`:298-345`). `tests/integration/win32-control.ps1:193-245` the #230 split checks. No
  `qa/panes.md` here — `agliteterm/qa/panes.md` is the template, with the rule "read the pane count
  from a `PrintWindow` capture; `session text` answers about one pane and cannot tell you how
  many there are".
- Undo-close (`ClosedSession`, `Program.Sessions.cs:1274-1277`) records no panes: `session reopen`
  of a split session brings back one pane today. Out of scope; noted.
- Open issues on this surface: **#209** (a restored/adopted pane keeps the host's old grid — the
  axis changes the grid a restored pane gets; do not make it worse, note it in the PR), #228/#234
  (same two files: `FakeSessionHost.cs`, `Program.ControlHost.cs` — merge friction, not overlap),
  #227 (`SessionOverlay` only — avoid).

## Constraints

- **Do not touch `tests/conformance/control-api.json`.** The `session split on --axis horizontal`
  step (result string = a pane id), a `session split close` step, a `session swap` step (object
  with `paneIds`), and the two refusals (`split close` on a single pane; `swap` on a single pane)
  land in the sibling contract PR after this merges — #235 after #233.
- **`session split off` must keep answering a bare string** — the existing conformance step is a
  type check on it. Every `session split` reply is a string (a pane id); only `swap` is an object.
- **Additive only**: one new key (`SessionState.Axis`, nullable, written only when horizontal so a
  vertical tree still saves the exact bytes today's build saves — `PreP3File_RoundTripsByteForByte`
  and `InMemoryTree_SerializesToThePreP3Bytes` stay green untouched), appended at the END of
  `SessionState` (the "property order is the file's key order" rule, `RestoreState.cs:28`); `axis`
  on the tree inside the split block; `SessionSnapshot.Axis` at the end.
- **Validated on load**: `RestoreState.LoadAxis(string?)` accepts exactly `vertical` /
  `horizontal` (case-sensitive, the wire spelling); anything else, and absent, is vertical.
- **Pane ids are durable and never reassigned** — by a split, a close, a swap, or a restore. A
  restore uses the saved ids verbatim; `CreateSession` accepts the pane-0 id separately from the
  session id. A `session new` still mints pane 0 = session id.
- **Two panes per session.** The axis is per session; a restore file with more than two panes
  restores the first two and logs the rest as dropped (say so in the format comment). N-generality
  in `PaneLayout` may stay; nothing new may depend on it.
- **No pty-host protocol change, no ABI change** (`tools/check-abi.ps1` v18 both sides).
- **Refusals leave the world untouched**; every host method that answers with state uses
  `InvokeOnUiQueued` and re-resolves inside the hop; a failed post or a closed window is a refusal
  (the #234 wording: a timeout leaves the action queued and says so).
- **Refusal logic and reply shapes live in `Agwinterm.Pty`** where the fake exercises them
  (`SplitAxes` for the axis words and their refusal; `SwapReply` for the object) — the
  `SidebarWidths` / `RestoreCaptureReply` shape.
- **#230 invariants above are non-negotiable**; the fake's split must mint an id (`AddPane`) or the
  returned id is untestable.
- Cross-cutting safety rules from `qa/product.md` in full: sandbox instance (`--pipe` and
  `--app-id`), never `keybd_event` / `SendInput`, `PrintWindow` never `CopyFromScreen`, never the
  real profile, `Restart-Sandbox` only under a minted sandbox app-id.

## Testing Strategy

- **Unit** — `tests/Agwinterm.Pty.Tests/SessionSplitTests.cs` (new; one file for the batch's four
  items, sectioned): the returned id names a pane in `paneIds`; `on` when on returns the same id
  and changes nothing; `off` returns pane 0's id; `--axis` accepted words, refused words (reported
  AND nothing split), re-orient live; `axis` in the tree only when split; `split close` by pane id
  either side, by session id = focused pane, single pane refused and nothing closed; `swap` — order
  reversed, focus follows, ratio sequence kept, ids kept, context/flag/overlay/status unchanged,
  single pane refused; a swapped session's save/load keeps its ids with no duplicates
  (`RestoreStateTests` twin). `RestoreStateTests.cs`: axis round-trip, old file → vertical, vertical
  writes no key, `LoadAxis` drops what the verb refuses, and the unknown-key fixture renamed.
- **Integration** — `tests/integration/win32-control.ps1`: `session split on --axis horizontal` →
  `session metrics` shows rows halved and cols not (the axis is provable from the grid); the
  returned id resolves; `split close` on pane 0 leaves pane 1 addressable by its old id; `swap`
  then `metrics` per pane shows the boxes unchanged and the ids on the other side.
  `tests/integration/restore-roundtrip.ps1`: `axis-graceful` and `axis-killed` cells (the killed
  one proves the axis was checkpointed at write time), and a `swap-killed` cell (pane order and
  ids survive).
- **QA** — `qa/panes.md` (new, from lite's template): a horizontal split stacks (`PrintWindow`
  capture, the divider is a horizontal hairline), the accent bar on the focused pane, `swap`
  exchanges content but not geometry (two captures, the divider position identical), the divider
  drag on a horizontal split, the cursor shape.
- Each task's checks pass before the next task starts.

## Progress Tracking

- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with ➕ prefix
- Document issues/blockers with ⚠️ prefix
- Update plan if implementation deviates from original scope

## Implementation Steps

### Task 1: `session split` returns the pane id, and the degenerate cases stop lying
- [x] `ISessionHost.Split(string? target, string op, string? axis)` returns `string` — the pane id,
      or `RefusePrefix` + a refusal (`session not found`, or the axis refusal from Task 2's
      `SplitAxes`). Comment states the vocabulary sentence, the reply per op (`on`/`toggle` → the
      split pane's id, also when already split; `off`/`toggle` → the survivor's id, also when
      already single), and the #230 invariants. `SingleSessionHost` refuses
- [x] host (`Program.ControlHost.cs:491-501`): resolve on the caller's thread for the unknown-target
      refusal, then **`InvokeOnUiQueued`** that re-resolves, calls `SplitOp(op, ses, axis)` and
      returns the id read back off `ses.Panes`; a hop that cannot be queued or run is a refusal
      (the #234 timeout wording)
- [x] server (`ControlServer.cs:254`): `Ok(id)` / `Err(refusal)`; `axis` read with a strict reader
      (`GetString`, then `SplitAxes.TryParse` — a non-string is refused, not defaulted)
- [x] fake: `Split` mints via `AddPane()` for `on`/`toggle`, removes for `off`, returns the id per
      the rules above; `SessionSplitTests.cs` section 1 (the id names a pane in `paneIds`; `on`
      twice = same id and `paneCount` still 2; `off` twice = pane 0's id; unknown target refused
      and no session changed; `session split` honours `--target` — the #230 tests move here or
      stay, but they must not be weakened)
- [x] CLI (`Ctl/Program.cs:276`): unchanged for `on|off|toggle`; the reply is printed as it is (the
      id) — confirm the `--json` and plain forms both show it; usage header names the reply
- [x] `tests/integration/win32-control.ps1`: the split block asserts the returned id equals the new
      entry in `paneIds`
- [x] run the .NET suite — must pass before task 2 (801 passed; `win32-control.ps1 -Strict` also green against a sandbox, both new split checks included)

### Task 2: the axis
- [x] `src/Agwinterm.Pty/SplitAxes.cs`: `Vertical = "vertical"`, `Horizontal = "horizontal"`,
      `TryParse(string?, out string axis, out string? refusal)` — the refusal names the value and
      both words; one comment stating what each word produces (left/right vs top/bottom, agterm's
      definition) — landed in Task 1 (the `Split` signature and the server's strict reader needed
      it); until this task lands the Win32 host refuses `horizontal` outright rather than laying it
      out vertical with ok:true (`Program.ControlHost.cs`, the "not laid out by this build" refusal —
      remove it here)
- [x] `Ses.Axis` (`Program.cs:332-360`; default vertical); `SplitOp(op, ses, axis)`: `on`/`toggle`
      with an axis sets it before laying out; **`on` with an axis on an already-split session
      re-orients it** (regrid, redraw, save) and returns the existing split id; `off` ignores the
      axis; toggle without an axis keeps the session's
- [x] `PaneLayout` (`Program.Sessions.cs:453-469`) becomes axis-aware: one function, one branch on
      `ses.Axis`, returning the same `(x, y, w, h)` tuples so the 11 callers do not change;
      `DividerW` renamed in comment to a thickness
- [x] `ResizeActiveSplitInternal` (`:1562-1583`): on horizontal, `growUp/growDown` in ROWS via cell
      height and `totalH`; `session resize` gains `--grow-top` / `--grow-bottom` (agterm's names),
      and `--grow-left` / `--grow-right` on a horizontal split are refused naming the axis (a
      request that cannot mean anything must not silently move the divider)
- [x] hit-tests: `FocusPaneAtX` → `FocusPaneAt(px, py)`, `DividerAtX` → `DividerAt(px, py)`,
      `DragDivider(px, py)` (and `Program.WndProc.cs:490` passing both), `PaneAt` (`:546-558`), the
      wheel test (`WndProc.cs:555-568`) — each branches on the session's axis through the layout
      tuples, never on its own arithmetic
- [x] renderer: the divider hairline drawn along the axis; the focused-pane accent bar —
      **decide**: keep it pinned to the title bar for vertical (unchanged pixels for the existing
      layout) and put it on the pane's own top edge for horizontal; state the reason in a comment
- [x] `WM_SETCURSOR`: `IDC_SIZEWE` / `IDC_SIZENS` over the divider by axis (new; small)
- [x] `DrawSplitGlyph`: the toolbar icon shows two side-by-side panes when vertical (or unsplit)
      and two stacked panes when horizontal
- [x] focus: `FocusPaneDir` accepts `primary|split|left|right|top|bottom|other` (agterm's list);
      `left`/`top` = pane 0, `right`/`bottom` = pane 1, `primary` = pane 0, `split` = pane 1,
      `other` = toggle; a direction that does not exist on the session's axis (`top` on a vertical
      split) is refused naming the axis; keymap ids unchanged, new `focus_top_pane` /
      `focus_bottom_pane` actions aliasing the same walk (no default chords — say why)
- [x] `SessionSnapshot.Axis` (end); `Tree()` in both hosts; `HandleTree` emits `"axis"` inside the
      `paneCount > 1` block, always; `Tree_OmitsSplitFields_ForSinglePane` extended to assert `axis`
      is absent for a single pane
- [x] CLI: `session split on|toggle --axis vertical|horizontal`; `session focus` new words;
      `session resize --grow-top/--grow-bottom`; usage header
- [x] `SessionSplitTests.cs` section 2: both words; `h`/`V`/`diagonal`/`""` refused (reported, and
      `paneCount` unchanged); re-orient live; `axis` in the tree; `focus top` on a vertical split
      refused; `--grow-left` on a horizontal split refused
- [x] a sandbox smoke with a `PrintWindow` capture of a horizontal split, attached to the task note —
      `docs/plans/2026-09-06-p4-horizontal-split.png` (isolated instance, `split on --axis horizontal`,
      `focus top`, `resize --grow-bottom 3`): the panes stack, the divider is a horizontal hairline, the
      accent bar sits under the title bar over the focused top pane, the toolbar glyph shows two stacked
      panes; `metrics` read 89x16 for both panes before the grow and 89x19 for the top pane after it;
      `focus left` was refused naming the axis. Decisions recorded in code: the accent bar is the
      line ABOVE the focused pane (title-bar edge on vertical — unchanged pixels — and for the bottom
      pane of a horizontal split the divider itself); `session focus` defaults to `other`, the one
      word valid on either axis, and refuses a one-pane session; `session resize` refuses a one-pane
      session too (both were silent successes)
- [x] run the .NET suite — must pass before task 3 (821 passed: 246 Core + 575 Pty, 20 new;
      `win32-control.ps1 -Strict` green with five new axis checks — rows halved / cols full from
      `metrics`, the same id answered, the refusals, and the exact grid back on `--axis vertical`)

### Task 3: the axis survives restore
- [x] `SessionState.Axis` (`string?`, end of the class, `WhenWritingNull`; saved as `"horizontal"`
      only when the session is horizontal AND split — a vertical or single-pane session writes no
      key, so today's files are byte-identical); `RestoreState.LoadAxis` beside `LoadContext`; the
      format comment names the key, the default and the two-pane rule — plus
      `RestoreState.StoreAxis(paneCount, axis)`, the one rule for WHEN the key is written (the P3
      pattern: the rule lives in `Agwinterm.Pty` where the format test can see it, and `SaveState`
      calls it rather than restating it)
- [x] `SaveState` / `TryRestoreState`: write and read it; a restored session regrids under its axis
      before its shells attach (the #209 surface — the grid a restored pane is given must be the
      axis's grid; note in the PR whether #209 got better, worse or neither) — the axis is set right
      after `CreateSession`, before `AppendPane` spawns pane 1 and before the `RegridSession` that
      sizes both ptys from `PaneLayout`. **#209: neither.** Every pty is spawned at the full grid
      and regridded to its box; a split pane's box differs from the full grid on EITHER axis, so an
      adopted split pane's `Resize` is sent as before, and the single-pane case #209 describes is
      untouched. The two-pane cap is enforced here too: a file listing more panes restores the first
      two and `Debug.WriteLine`s the drop (the loop stays N-general; nothing depends on it). A
      single-pane session's `Axis` key is ignored on load and never written back
- [x] `RestoreStateTests.cs`: `RoundTrip_PreservesAxis`; `OldShapeFile_LoadsVertical`;
      `VerticalSession_WritesNoAxisKey` (the pre-P3-bytes twin); `LoadAxis_DropsWhatTheVerbWouldRefuse`
      (`"h"`, `"Horizontal"`, `""`, `"diagonal"` → vertical); **rename the unknown-key fixture's
      `"SplitAxis"` / `"Axis"` to keys that are still unknown** and say why in the test — renamed to
      `Orientation` / `Tilt` with the reason; also `Axis_IsTheLastKeyOfTheSession`,
      `StoreAxis_WritesTheKeyOnlyForASplitHorizontalSession`,
      `OutOfRulesAxis_InAFile_SurvivesTheParseAndIsDroppedOnLoad` and
      `HorizontalKey_OnASinglePaneSession_LoadsButMeansNothing` (Task 7's edge case, pinned at the format)
- [x] `tests/integration/restore-roundtrip.ps1`: `axis-graceful`, `axis-killed` — split
      `--axis horizontal`, assert `tree` and the file, restart, assert `tree` again and `session
      metrics` (rows halved) — the idle single-pane session is the ruler: both restored panes have
      its columns and at most ⌈rows/2⌉ rows; the same two pane ids come back in the same order; the
      file written after the restore carries the key exactly once
- [x] run the .NET suite and the round-trip script — must pass before task 4 (835 passed: 246 Core +
      589 Pty, 14 new; `restore-roundtrip.ps1 -Strict` all six cells green, 47 checks)

### Task 4: `session split close`
- [x] `ISessionHost.SplitClose(string? target)` → the survivor's id or a refusal; server case
      `session.split.close` beside `session.split` (the CLI spells it `session split close
      [--target ID]`; `close` is a sub-op of `split`, so `Ctl/Program.cs:276`'s `op` positional
      routes it — an `op` of `close` becomes `cmd = "session.split.close"`) — the refusal wordings
      live in `src/Agwinterm.Pty/SplitCloseReply.cs` (unknown target, no active session, single pane
      naming `session close`, cover), the `RestoreCaptureReply` shape, so the fake gives the app's sentences
- [x] host: resolve with `FindControlPane` (a pane id → that pane; a session id / name / no target
      → the session's focused pane); refuse a cover pane (`CoverPane` wording, like
      `restore.capture`); refuse a single-pane session naming `session close`; in the hop:
      `ClosePane(ses, pane)` — the `CloseActivePane` primitive generalised to a pane argument
      (dispose that pane's `ISession`, redistribute the ratio to the survivor, survivor becomes
      `Panes[0]` with `Active = 0`, `_session` repointed only if active, regrid, redraw, save,
      `EmitEvent("tree")`); `CloseActivePane` and `CollapseToSinglePane` call it (one primitive,
      three callers); `session split off` keeps its "pane 0 survives" rule (agterm's `off` semantics
      minus the hide). ⚠️ Resolution as implemented follows `FindControlPane`'s order literally:
      exactly one pane carries the session id, so a target of the session id lands on THAT pane
      through the exact-pane arm (the order `win32-control.ps1` pins), and "a session id → the
      focused pane" is reached only by a session NAME (or no target / `active`). Stated in
      `ISessionHost.SplitClose`, `SplitCloseReply` and the CLI header. ➕ `OnPaneProcessExited` is a
      fourth caller of `ClosePane` (it duplicated the same removal), so a split shell's exit now also
      emits the tree event; the pane is removed from the list before its shell is disposed (#85)
- [x] fake: `SplitClose` with the same resolution; `SessionSplitTests.cs` section 3: pane 0 closed →
      pane 1's id survives as `paneIds[0]` and is still resolvable; pane 1 closed; session id →
      focused pane; single pane refused with `session close` named and `paneCount` unchanged;
      a cover refused — 9 tests; the fake's `FindPane` gained the exact-session and session-prefix
      arms (`FindControlPane`, arm for arm), so a session whose id-pane was closed still resolves to
      its survivor, as the app's does (the "promoted" case the integration script pins); `Sess.RemovePane`
      is the fake's one primitive behind `off` and `close`
- [x] `tests/integration/win32-control.ps1`: close pane 0 by id, then `session text --target
      <old pane 1 id>` still answers — on a `--no-select` fixture of its own: a marker typed into pane 1
      before the close reads back after it by the old pane id AND by the session id; the second
      `split close` is refused naming `session close`
- [x] `docs/agterm-parity.md` item 3's "naming, not behaviour" line corrected in the tracker edit
      of Task 6 — corrected now (the sentence was false; Task 6 moves the item into the Closed table)
- [x] run the .NET suite — must pass before task 5 (844 passed: 246 Core + 598 Pty, 9 new;
      `win32-control.ps1 -Strict` all 74 checks green, the four new split-close checks included)

### Task 5: `session swap`, and ids that survive it
- [ ] `src/Agwinterm.Pty/SwapReply.cs`: `Build(sessionId, paneIds, focusedPane, axis)`;
      `SinglePane(sessionId)` refusal wording; `ISessionHost.Swap(string? target)` →
      `SwapResult` / refusal
- [ ] host: resolve (a session or either of its panes); in the hop: under `lock (_workspaces)`
      reverse `Panes`, `Active = Count-1-Active`, **keep the ratio sequence** (swap the two panes'
      `Ratio` values so the left/top box keeps its size — the agterm rule "axis and ratio remain
      fixed"), reset `_divLeft`, regrid both panes (their grids change), `_session` only if active,
      redraw, save, `EmitEvent("tree")`; nothing else moves — a comment enumerates what was checked
      and found per-session or order-independent (overlays, status aggregate, context, flag, MRU,
      broadcast, UIA)
- [ ] **durable ids**: `CreateSession` takes the pane-0 id separately (callers other than restore
      pass the session id); `TryRestoreState` creates pane 0 with `pl[0].Id` and appends the rest
      with their saved ids, so a swapped-then-saved session restores with its ids intact and no
      duplicates; the "first pane shares the session id" comments (`Program.Sessions.cs:421`,
      `FakeSessionHost.cs:30-34`, `ISessionHost`) become "exactly one pane carries the session id,
      and a swap may put it on either side"; the resolver's exact-pane-first order is what keeps
      the session id naming that pane — say so where the order is defined (`:1141-1151`)
- [ ] fake: `Swap` with the same rules; `SessionSplitTests.cs` section 4: order reversed; focus
      follows the pane; ratios swapped so the sequence is kept; ids unchanged and both resolvable;
      context / flagged / overlay / status / `statusChangedAt` unchanged (assert each); single pane
      refused and nothing changed; a cover target refused; **swap → serialize → deserialize keeps
      the ids** (`RestoreStateTests` or here — the app's restore path is Win32, so the unit test
      pins the fake and the format; the integration cell pins the app)
- [ ] `tests/integration/win32-control.ps1`: `swap`, then `metrics` per pane — each pane's box is
      the other's old box, ids on the other side; `tests/integration/restore-roundtrip.ps1`:
      `swap-killed` — a swapped session restores with the same `paneIds` order and no duplicate
- [ ] CLI `session swap [--target ID]`; usage header
- [ ] run the .NET suite and both integration scripts — must pass before task 6

### Task 6: docs, trackers, QA
- [ ] `AgentSkill.cs`: the split section rewritten — the reply is the pane id (and what `on`
      returns when already split), `--axis` with the one-sentence definition, `split close`
      (either side; a single pane is refused), `swap` (what moves and what does not, ids never),
      `focus` words, `resize --grow-top/--grow-bottom`
- [ ] `README.md`: the example block and the prose list; `docs/agterm-gap-analysis.md:47`'s
      "horizontal" corrected to agterm's vocabulary
- [ ] `docs/agterm-parity.md`: items 2 and 3 into the Closed table with the PR number; the two
      divergences recorded (`off` destroys; a swap moves panes, never ids); `docs/lite-parity.md`:
      strike "`session.split` returns the split's id — agwinterm should copy this" (`:211-213`),
      and add a sentence at `:254-257` that P4's swap is the first item lite's split-as-hidden-
      session model makes expensive
- [ ] `qa/panes.md` (new; lite's `qa/panes.md` as the template, with its `PrintWindow` rule):
      the horizontal split, the accent bar, the divider drag and cursor, `swap` (two captures,
      divider position identical), `split close` on pane 0
- [ ] `docs/plans/2026-09-03-parity-batches.md`: nothing until the final task

### Task 7: [Final] Verify acceptance criteria
- [ ] every item in Overview implemented; the vocabulary sentence identical in the three places
- [ ] edge cases: `split on --axis horizontal` on a session whose window is 30 rows tall (each
      pane gets ≥ 1 row; the ptys are told the truth); `split close` on the pane whose shell
      already exited; `swap` while a divider drag is in progress (the drag ends, nothing tears);
      `swap` twice = identity (ids, order, ratios, focus); `split off` after a swap (pane 0 — the
      former split shell — survives, the session id's pane is destroyed: correct per the rules,
      and the reply names the survivor); a restore file with three panes (two restored, one
      logged); a restore file with `"Axis":"horizontal"` on a single-pane session (ignored, no key
      written back); `session focus top` on a vertical split (refused); `--grow-left` on a
      horizontal split (refused, divider unmoved); `session reopen` of a closed split session
      (one pane, as today — unchanged and documented)
- [ ] confirm `tests/conformance/control-api.json` is **unchanged** and `conformance.ps1 -Strict`
      passes — the `split off` step's string check, on a pane id
- [ ] run the full .NET suite, the Rust suite, `tools/check-abi.ps1` (v18), `win32-control.ps1`
      and `restore-roundtrip.ps1` end to end against a sandbox
- [ ] a state file saved by this build with a horizontal, swapped session loads in the released
      **0.17.12** under a sandbox app-id: no `.bad`, both panes restored side by side (the axis key
      dropped, the pane ids kept), and the key dropped on its next save — the downgrade is loss,
      checked once
- [ ] mark P4 **Shipped** in `docs/plans/2026-09-03-parity-batches.md` with the PR number

## Technical Details

- **Why the reply stays a string.** The conformance file's `split off` step is a `"result":"string"`
  type check; a pane id is a string, so the change is invisible to the contract while giving the
  caller the handle it lacked. An object would have broken a shipped step for a field (`axis`)
  the tree already carries.
- **Why `on` when already split returns the existing id.** lite's rule, and the only honest
  answer that is also useful: `ok:true "split"` for a no-op was the P2 defect class, and a refusal
  would make every "make sure this session is split" script branch on an error.
- **Why the axis is per session and two panes are the cap.** agterm's model (primary + split), our
  `SplitPane` guard, and the ratio semantics all assume two; a per-pane axis is a tree layout,
  which is not the item. Restore tolerating three panes today is an accident, now a documented
  drop.
- **Why the ratio sequence is kept on a swap.** `Pane.Ratio` is each pane's own share, so reversing
  the panes alone makes a 70/30 into a 30/70 — the layout jumps. agterm says "axis and divider
  ratio remain fixed"; keeping the sequence (exchanging the two panes' values) is that rule in our
  representation.
- **Why ids never move.** Our peer tooling addresses panes by id (the tracker's own words). An id
  that changed meaning on a swap would be a handle that lies. The cost is one relaxed invariant —
  the session id may sit on either side — which the resolver's exact-pane-first order already
  handles, and which restore must honour by never re-minting pane 0's id.
- **Why `split close` on a single pane refuses.** `session close` exists; a `split close` that
  quietly closed the session would be the silent-success class one verb over.
- **Why the accent bar moves for horizontal only.** For vertical the existing pixels are the
  contract of every capture and screenshot so far; for horizontal a bar pinned to the title bar
  would mark the wrong pane half the time.
- **agterm's `off` hides; ours destroys** — unchanged, pre-existing, recorded. `split close` is
  agterm's destroying verb and ours; the difference is only what `off` does.

## Post-Completion

*Informational — no checkboxes*

- **Sibling contract PR** after this merges: `session split on --axis horizontal` (result string;
  it may `capture` the id for a following `session split close --target {id}` step), `session
  swap` (object with `session`, `paneIds`, `focusedPane`, `axis`), errors: `session split close`
  and `session swap` on a single-pane session, `session split on --axis diagonal`. agliteterm's
  `check-contract` goes red for the gap until P4-lite — expected.
- **Release** after the contract PR (P4 adds verbs). `0.17.18` is the package checkpoint; the next
  number is 0.17.13 — Boris calls the tag, and lite's PR CI is red by design until it exists (one
  failure email per push — tag promptly).
- **P4-lite**: the axis is cheap there (a 5th field on the `P` line; `paneRect` is a two-line
  halving with no ratio and no drag); `split close` needs "promote the hidden session to visible"
  when pane 0 closes (lite's `hidden` is one-way today); **`swap` is expensive in lite's model** —
  a hidden session and its visible owner would have to exchange tree identity (name, context,
  flag, workspace position, the `D`/`C`/`K` lines) or invert `splitId` and both `hidden` flags —
  consider deferring lite's swap the way `session.restore` went to P9, and say so in the batch
  index rather than letting P4-lite discover it.
- **P5 re-opens swap**: once overlays are pane-scoped, a swap must move them. P5 inherits that
  obligation from this note.
- **#209** sits under Task 3; report its state in the PR.
- Review: **revmux**, two rounds minimum, and a narrow round for each fix commit.
